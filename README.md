# ChromaStrap
ChromaStrap is a lightweight CSS extension for Bootstrap that injects a massive palette of vibrant, modern, and "super cool" colors into the standard framework. It mimics Bootstrap's native utility logic, providing dozens of new classes for text, backgrounds, buttons, and borders without changing the way you code.
